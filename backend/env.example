# Clermont Backend – copy to .env.development and .env.production
# Backend loads .env.{NODE_ENV} (development or production)

# -----------------------------------------------------------------------------
# Supabase (Clermont project – get from Supabase Dashboard → Settings → API)
# -----------------------------------------------------------------------------
SUPABASE_URL=https://YOUR_CLERMONT_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your_clermont_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_clermont_service_role_key

# -----------------------------------------------------------------------------
# Resend (email – dev vs prod like Stripe: use test key in .env.development, live in .env.production)
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=Clermont <onboarding@resend.dev>
# Admin notifications (e.g. new signup) – only sent when NODE_ENV=production
ADMIN_EMAIL=you@example.com

# -----------------------------------------------------------------------------
# Stripe (use Lindero keys first to verify; later replace with Clermont Stripe)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# -----------------------------------------------------------------------------
# Server & frontend
# -----------------------------------------------------------------------------
PORT=3002
FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Geo / external APIs (use Lindero keys to verify; later replace with new keys)
# -----------------------------------------------------------------------------
GEOSERVICE_API_KEY=your_geoservice_api_key
